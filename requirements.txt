# Phase 1 uses only the Python standard library (urllib for Ollama HTTP calls).
# Kept as an explicit file so Docker/dev setups have a pinned install step ready
# for Phase 2 dependencies (e.g. requests, pydantic, pytest).
